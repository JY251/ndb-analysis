[project]
name = "ndb-its-analysis"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pdfplumber>=0.11",
    "openpyxl>=3.1",
    "requests>=2.32",
]

[tool.uv]
dev-dependencies = []
